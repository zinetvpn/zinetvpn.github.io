# مشارکت

برای اصلاح محتوا Issue بسازید و منبع رسمی را پیوند دهید. هر صفحه باید هدف جست‌وجوی مستقل، یک H1، فراداده یکتا و CTA معتبر داشته باشد. فایل اجرایی، لینک اشتراک، داده مشتری یا Secret پذیرفته نمی‌شود. پیش از Pull Request فرمان `npm run build` را اجرا کنید.
