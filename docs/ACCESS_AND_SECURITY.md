# دسترسی و امنیت

Workspace در شروع Git repository نبود و GitHub connector نصب نبود. درخواست مستقیم `github.com/zinetvpn` در ۱۳ سپتامبر ۲۰۲۶ پاسخ 404 داد؛ این فقط نبود پروفایل عمومی را نشان می‌دهد و رزروشدن نام یا امکان ساخت Organization را تضمین نمی‌کند. هیچ Secret دریافت یا ذخیره نشد. انتشار به ایجاد مخزن عمومی `zinetvpn.github.io` و انتخاب GitHub Actions در Pages نیاز دارد.

پیش از Release: اسکن Secret و History، بازبینی Dependabot/npm audit، تأیید CODEOWNERS، و فعال‌کردن حداقل مجوز Workflow. فایل اجرایی و داده مشتری در این مخزن ممنوع است.
